import * as React from "react";
import { styled, Box } from "@mui/material";

interface Props {

}

const ComponentTemplate = (props: Props) => {
    return (
        <Box>
            
        </Box>
    );
}