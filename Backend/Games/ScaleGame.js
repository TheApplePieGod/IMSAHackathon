
// Define message type enum for all messages for this game
const MessageType = {
    None: 0
};
Object.freeze(MessageType);

exports.handleMessage = (messageType, data) => {
    switch (messageType) {
        default: break;
        
    }
}